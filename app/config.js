// PulsePage frontend config.
// Default to the live API so Android installs work immediately.
// The hosted web app can also use this safely because the API allows CORS.
window.PULSEPAGE_API_BASE_URL = window.PULSEPAGE_API_BASE_URL || 'https://api.getpulsepage.com';
window.PULSEPAGE_ADMIN_TOKEN = window.PULSEPAGE_ADMIN_TOKEN || '';
